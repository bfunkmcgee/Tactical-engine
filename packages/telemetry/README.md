# telemetry

Gameplay instrumentation events for analytics and balance tuning.

## Canonical events

- `action_pick`: chosen action per player turn.
- `match_result`: final win/loss outcome and turn count.
- `turn_duration`: per-turn runtime latency.
- `ability_usage`: ability activations and targets.
